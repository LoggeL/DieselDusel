services:
  dieseldusel:
    build: .
    container_name: dieseldusel
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - dieseldusel_data:/app/data
    networks:
      - proxy

networks:
  proxy:
    external: true

volumes:
  dieseldusel_data:
